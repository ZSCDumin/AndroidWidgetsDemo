<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edu.zscdm.androidwidgetsdemo">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TextViewTest" />
        <activity android:name=".TextSwitchAndImageSwitch" />
        <activity android:name=".ButtonTest" />
        <activity android:name=".ViewPagerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ToggleButtonTest" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SeekBarActivity" />
        <activity android:name=".SpinnerActivity" />
        <activity android:name=".ViewFlipperTest"/>
    </application>

</manifest>